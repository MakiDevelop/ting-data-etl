# fan-out by storeId / shopId

本專案用於處理大量 CSV 原始資料，依據指定的主鍵欄位（例如 `storeId` 或 `shopId`），進行 **row-level fan-out**，並將資料彙整成以主鍵為單位的目錄結構，供後續流程（如 Google Apps Script、Gemini、簡報自動化等）使用。

---

## 使用情境說明

典型情境如下：

- 有多個來源 CSV（例如 60 個），欄位結構不一致
- 每個 CSV 中包含多筆不同 `storeId / shopId`
- 需要：
  - 先依主鍵拆分資料列
  - 再將同一主鍵的資料集中管理
  - 並能快速驗證拆分結果是否正確

---

## 專案結構

```
.
├─ fan_out_by_storeid.py     # 依主鍵拆分 CSV（row-level fan-out）
├─ verify_fanout.py          # 驗證 fan-out 結果完整性與正確性
├─ input/                    # 原始 CSV 放置目錄
│   ├─ data_01.csv
│   ├─ data_02.csv
│   └─ ...
└─ output/                   # fan-out 後輸出目錄
   ├─ store_001/
   │  ├─ data_01.csv
   │  └─ data_03.csv
   ├─ store_002/
   │  └─ data_02.csv
   └─ ...
```

---

## 步驟一：依主鍵拆分 CSV（fan-out）

在專案根目錄執行：

```bash
python fan_out_by_storeid.py --input-dir ./input --output-dir ./output --encoding utf-8
```


### 行為說明

- 逐檔、逐列讀取 `input/` 底下的 CSV
- 動態尋找主鍵欄位（目前預設為 `storeId`，實際可依資料調整）
- 每一列資料依主鍵值輸出到：

```
output/{storeId}/{原始檔名}.csv
```

- 不使用 pandas
- 不保留 file handle（即開即寫即關），可安全處理大量檔案

#### 商店序號欄位名稱支援（Header Alias）

由於不同資料來源、報表工具與人員習慣不一，  
同一語意的「商店序號」欄位，實務上可能以不同名稱出現。

目前 `fan_out_by_storeid.py` 內建支援以下欄位名稱作為商店識別鍵：

- `商店序號`
- `shopId`
- `Shop 商店序號`

只要 CSV header row 中出現上述任一名稱，即會被視為合法的 fan-out 依據。  
若皆未出現，該檔案將被視為**不具備商店層級語意**並略過處理。

---

### 為什麼要掃描 header row，而不是假設第一列

本專案**不假設 CSV 的第一列一定是 header**，而是會逐列往下掃描，找到實際包含主鍵欄位（例如「商店序號」）的那一列，才視為真正的 header。

這是因為實務上，許多 CSV 來源並非「純資料檔」，而是由 BI 工具或報表系統匯出，常見會在檔案最前方加入一至多列的 **meta row**，例如：

- 年份／期間說明（如 `Established At Year, 2025, 2024`）
- 群組標題、報表註解
- 給人類閱讀的說明列，而非實際欄位名稱

若假設第一列即為 header，將會導致：
- 主鍵欄位無法被正確解析
- 整支檔案被誤判為格式錯誤而跳過
- fan-out 與驗證結果出現「看起來缺資料，但實際原始檔有資料」的假錯誤

因此，本專案採用 **header 掃描策略**：
- 往下尋找第一列包含主鍵欄位名稱的列
- 將其視為 header
- header 之前的列視為 meta row，於 fan-out 時原樣保留至輸出檔案中

此設計可確保：
- 正確處理多層 header 的報表型 CSV
- 保留原始報表語意，避免破壞下游使用情境
- fan-out 與 verify 對 CSV 結構的理解保持一致

- 逐檔、逐列讀取 `input/` 底下的 CSV
- 動態尋找主鍵欄位（目前預設為 `storeId`，實際可依資料調整）
- 每一列資料依主鍵值輸出到：

```
output/{storeId}/{原始檔名}.csv
```

- 不使用 pandas
- 不保留 file handle（即開即寫即關），可安全處理大量檔案

---

## 步驟二：驗證 fan-out 結果

完成拆分後，建議立即執行驗證腳本：

```bash
python verify_fanout.py --input-dir ./input --output-dir ./output --encoding utf-8
```

### 驗證內容包含

1. **檔名完整性**
   - 檢查每個 store/shop 目錄下的 CSV 檔名是否合理
2. **資料正確性**
   - 驗證每一列資料的 `storeId / shopId` 是否與資料夾名稱一致
3. **快速回饋**
   - 驗證速度快，可在大量檔案情境下反覆執行

若驗證失敗，程式會回傳非 0 的 exit code，適合接入自動化流程。

---

## 補充驗證工具：輸出資料夾檔案數檢查（verify_store_file_count.py）

在實務上，fan-out 或後續分析流程若出現異常，常見徵兆之一是：
**同一個商店序號目錄底下，出現超出預期數量的檔案**。

例如：
- 商店序號被誤判為 `40316` 與 `40316.0`
- 同一分析被重複輸出
- 上游資料異常導致 fan-out 分流錯誤

為避免人工逐一檢查，本專案提供專用驗證工具：`verify_store_file_count.py`。

---

### 功能說明

- 遞迴掃描整個 `output/` 目錄
- 將「**底下直接包含檔案的資料夾**」視為一個商店輸出單位
- 檢查每個資料夾的檔案數是否超過指定上限
- 僅輸出異常資料夾路徑，作為最終稽核結果
- 支援盤點模式（--list-all），列出所有商店資料夾及其檔案數量

---

### 使用方式

在專案根目錄執行：

```bash
python verify_store_file_count.py \
  --input-dir ./output \
  --limit-file-count 1
```

#### 盤點模式（list-all）

若僅需盤點整體輸出結構，而不進行 gate 檢查，可使用 `--list-all`：

```bash
python verify_store_file_count.py \
  --input-dir ./output \
  --limit-file-count 1 \
  --list-all
```

此模式會列出所有「底下直接包含檔案」的資料夾及其檔案數量，  
不套用 limit 條件，且一定回傳成功（exit code = 0）。

---

### 輸出範例

```text
output/2024/Q1/40316 exceeded limit
output/2024/Q2/1194 exceeded limit
```

若存在任何異常，程式會回傳非 0 的 exit code，  
可直接作為自動化流程或人工稽核的 gate 使用。

---

### 設計定位說明

此工具定位為 **最終結果驗證層（final sanity check）**：

- 不檢查資料內容
- 不檢查檔名語意
- 不嘗試推論錯誤原因

僅回答一個問題：

>「有哪些資料夾的輸出結果，已經不符合預期？」

---

## 步驟三：依商店序號產生分析輸出（aggregate_by_store.py）

---

## 補充工具：CSV 分段合併器（csv_chunk_merger.py）

在實務上，部分 BI / 報表系統在匯出 CSV 時，會因為 **單檔筆數限制**，  
將同一份報表自動拆分為多個檔案，例如：

```
20-1.會員app下載與line綁定分析_app下載率卡片.csv
20-1.會員app下載與line綁定分析_app下載率卡片(1).csv
20-1.會員app下載與line綁定分析_app下載率卡片(2).csv
20-1.會員app下載與line綁定分析_app下載率卡片(3).csv
```

這類檔案在語意上屬於「**同一份資料的 row-level chunk**」，  
必須先合併回單一 CSV，才能進行後續 fan-out、驗證或分析。

為此，本專案提供獨立工具：`csv_chunk_merger.py`。

---

### 功能說明

- 自動辨識同一組 CSV 分段檔案（依檔名 + `(n)` 規則）
- 依序合併資料列（縱向 concat）
- **不修改、不轉換、不補欄位**
- 僅負責「結構一致 CSV 的安全合併」

---

### 使用方式

在專案根目錄執行：

```bash
python csv_chunk_merger.py \
  --input ./input_chunks \
  --output ./merged_csv
```

### 合併規則定義

- `xxx.csv` 視為第 0 段
- `xxx(1).csv`, `xxx(2).csv` 依數字排序後接續
- 欄位結構必須一致，否則程式會直接失敗（刻意設計）
- 合併後輸出檔名固定為：

```
{原始 base 檔名}.csv
```

---

### 設計定位說明

此工具刻意維持極小責任範圍：

- 不負責資料清洗
- 不處理欄位差異
- 不嘗試「猜測」正確結構

其目的僅為 **將「系統切開的資料」還原為「可被處理的原始單元」**，  
屬於資料前處理流程中，最上游、最低風險的一步。

```
BI / 報表系統
    ↓（筆數限制）
CSV chunk files
    ↓（csv_chunk_merger.py）
單一完整 CSV
    ↓
fan-out / verify / aggregate
```

此腳本用於在 **fan-out 完成後**，針對單一商店序號，產生多種分析結果（23–25 系列），  
每一個分析會輸出為一個獨立的 CSV，並放置於該商店序號目錄底下。

### 指令範例

```bash
python aggregate_by_store.py --config 23-1
python aggregate_by_store.py --config 23-2
python aggregate_by_store.py --config 24-1
python aggregate_by_store.py --config 24-2
python aggregate_by_store.py --config 25-1
python aggregate_by_store.py --config 25-2
```

### 輸出結構

```
output/
└─ {商店序號}/
   ├─ 23-1.csv
   ├─ 23-2.csv
   ├─ 24-1.csv
   ├─ 24-2.csv
   ├─ 25-1.csv
   └─ 25-2.csv
```

> ⚠️ 並非每一個商店都會產生全部分析檔案。  
> 若該分析所需的「必要資料不存在」（例如門市層級資料缺失），  
> 則該分析 **不會產生輸出檔案**，以避免產生誤導性結果。

### 各分析簡述（語意層）

- **23-1**：年度推薦人綁定 KPI（總量、YoY、推薦人綁定率）
- **23-2**：推薦人綁定月別 YoY 趨勢
- **24-1**：推薦人績效 KPI（年度）
- **24-2**：門市 × 月份 推薦人轉換率（門市層級）
- **25-1**：門市結構分析（每商店 Top 5 門市，依佔比 DESC）
- **25-2**：門市結構分析（每商店 Bottom 5 門市，依佔比 ASC）

```

---

## 步驟四：驗證指定商店是否具備分析所需資料（verify_store_presence.py）

此腳本用於 **快速檢查指定商店序號**，在六個核心資料來源中是否存在資料列，  
以解釋「為何某些分析檔案未產生」。

### 使用時機

- 某商店只產生部分分析檔案
- 想確認是資料缺失，還是程式錯誤
- 作為分析前的資料可用性檢查（pre-check）

### 指令範例

```bash
python verify_store_presence.py --store 1194
python verify_store_presence.py --store 40316
```

### 輸出說明（範例）

```text
🔍 檢查商店序號：40316

✅ 區間推薦人綁定資料：有資料
⛔ 門市首購人數（月別）：沒有資料
⛔ 門市首購人數（門市）：沒有資料
```

### 解讀原則

- **23 / 24 系列**：只要商店層級資料存在，即可產生
- **24-2 / 25 系列**：需要門市層級資料，若缺失則不產生輸出

此腳本本身不做任何分析或計算，僅負責回答：

>「這個商店，能不能做哪些分析？」

```

---

## 注意事項

- 目前主鍵欄位名稱以 `storeId` 為預設，實際使用時請依真實資料調整
- CSV 檔名可為中文，Python 3 / macOS 環境可正常處理
- 本專案僅負責 **資料拆分與驗證**
  - 後續 Google Drive 搬移、Apps Script、Gemini 處理不在此專案範圍內

---

## 後續流程（不在本專案內）

- 依 Google Sheets 對照表（shopId → Drive folder）
- 使用 Google Apps Script 搬移 fan-out 後的資料夾
- 交由 Gemini 產生簡報或文字內容

本專案定位為 **資料前處理與穩定性驗證層**。

```
python3 -m venv .venv
source .venv/bin/activate
pip install pandas
```

```
source .venv/bin/activate
python aggregate_by_store.py --config 23-1
python aggregate_by_store.py --config 23-2
python aggregate_by_store.py --config 24-1
python aggregate_by_store.py --config 24-2
python aggregate_by_store.py --config 25-1
python aggregate_by_store.py --config 25-2
```